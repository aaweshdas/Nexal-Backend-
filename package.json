{
  "name": "nexal-backend",
  "version": "1.0.0",
  "description": "Unified backend gateway — ARIA + Search via single Render port",
  "private": true,
  "type": "module",
  "main": "dist/gateway.js",
  "scripts": {
    "postinstall": "npm install --prefix aria_backend && npm install --prefix search_backend && npm install --prefix game_backend",
    "build": "npm run build --prefix aria_backend && npm run build --prefix search_backend && npm run build --prefix game_backend && tsc",
    "start": "node dist/gateway.js",
    "dev": "tsx src/gateway.ts",
    "aria": "npm run dev --prefix aria_backend",
    "search": "npm run dev --prefix search_backend",
    "game": "npm run dev --prefix game_backend"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  },
  "devDependencies": {
    "@types/http-proxy": "^1.17.16",
    "@types/node": "^20.12.7",
    "concurrently": "^8.2.2",
    "tsx": "^4.7.2",
    "typescript": "^5.4.5"
  }
}
